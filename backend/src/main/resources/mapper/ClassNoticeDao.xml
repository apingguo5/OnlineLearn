<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rabbiter.ol.dao.ClassNoticeDao">
    <resultMap id="baseMap" type="com.rabbiter.ol.entity.ClassNoticeEntity">
        <id column="id" property="id" />
        <result column="class_id" property="classId" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="notice_type" property="noticeType" />
        <result column="sender_id" property="senderId" />
        <result column="is_pinned" property="isPinned" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <sql id="baseColumns">
        cn.id,
        cn.class_id AS classId,
        cn.title,
        cn.content,
        cn.notice_type AS noticeType,
        cn.sender_id AS senderId,
        cn.is_pinned AS isPinned,
        DATE_FORMAT(cn.create_time, '%Y-%m-%d %H:%i:%s') AS createTime
    </sql>

    <select id="queryCount" resultType="java.lang.Integer">
        SELECT COUNT(*) AS total
        FROM class_notice cn
        LEFT JOIN `user` u ON u.id = cn.sender_id
        LEFT JOIN class c ON c.id = cn.class_id
        <where>
            <if test="classId != null">
                AND cn.class_id = #{classId}
            </if>
            <if test="senderId != null">
                AND cn.sender_id = #{senderId}
            </if>
        </where>
    </select>

    <select id="queryData" resultType="java.util.HashMap">
        SELECT
        u.user_name AS senderName,
        c.class_name AS className,
        <include refid="baseColumns"></include>
        FROM class_notice cn
        LEFT JOIN `user` u ON u.id = cn.sender_id
        LEFT JOIN class c ON c.id = cn.class_id
        <where>
            <if test="classId != null">
                AND cn.class_id = #{classId}
            </if>
            <if test="senderId != null">
                AND cn.sender_id = #{senderId}
            </if>
        </where>
        ORDER BY cn.is_pinned DESC, cn.create_time DESC
        LIMIT #{page}, #{pageSize}
    </select>

    <select id="queryByStudentClasses" resultType="java.util.HashMap">
        SELECT
        u.user_name AS senderName,
        c.class_name AS className,
        <include refid="baseColumns"></include>
        FROM class_notice cn
        LEFT JOIN `user` u ON u.id = cn.sender_id
        LEFT JOIN class c ON c.id = cn.class_id
        INNER JOIN user_class uc ON uc.class_id = cn.class_id AND uc.user_id = #{userId}
        <where>
            <if test="classId != null">
                AND cn.class_id = #{classId}
            </if>
        </where>
        ORDER BY cn.is_pinned DESC, cn.create_time DESC
        LIMIT #{page}, #{pageSize}
    </select>

    <select id="countByStudentClasses" resultType="java.lang.Integer">
        SELECT COUNT(*) AS total
        FROM class_notice cn
        INNER JOIN user_class uc ON uc.class_id = cn.class_id AND uc.user_id = #{userId}
        <where>
            <if test="classId != null">
                AND cn.class_id = #{classId}
            </if>
        </where>
    </select>
</mapper>
